# ============================================================
# Phoenix Configuration (v2)# Loaded by phoenix-core.sh for all WSL modules
# ============================================================

# ---------- Node Role ----------
# MASTERZERO | SECONDARY | OFFSITE
NODE_ROLE="SECONDARY"

# ---------- Syncthing API ----------
# Full REST endpoint (no trailing slash)
API_URL="http://192.168.1.246:8384/rest"

# Syncthing API key (from GUI or config.xml)
API_KEY="XzawtiD7rsiiNP69XtSkXqd6Mb5M9LpY"

# ---------- Mesh Peer Target ----------
# The peer Syncthing node for QUIC packets
PEER_IP="10.10.10.2"
PEER_PORT="22000"

# ---------- Local Device ID (optional) ----------
# Used by verify module for device ID checks
LOCAL_DEVICE_ID="HXEL3PJ-2O3W4L2-LSD6ILI-NPYBJUJ-IPPZPXE-K2RL6WH-KBJ5QLE-LO7HTAV"

# ---------- Future Expansion ----------
# Reserved for Phoenix v3 cluster-wide settings
